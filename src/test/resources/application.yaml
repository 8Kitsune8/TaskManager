spring:
  application:
    name: taskmanager
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  h2:
    console.enabled: true
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
jakarta:
  validation:
    notEmptyMessage: "cannot be empty u lazy one"
logging:
  level:
    root: INFO
